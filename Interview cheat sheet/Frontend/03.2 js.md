Here’s a clean, exam-ready + interview-friendly ES6 (ECMAScript 2015) Cheat Sheet 🔥
Perfect for JavaScript, React, Node.js & Full-Stack preparation.


---

🚀 ES6 (ECMAScript 2015) Cheat Sheet


---

1️⃣ let & const

let count = 10;      // block scoped
const PI = 3.14;    // cannot be reassigned

✔️ var → function scoped (avoid)
✔️ let/const → block scoped


---

2️⃣ Arrow Functions ⭐

const add = (a, b) => a + b;

const square = x => x * x;

✔️ Shorter syntax
✔️ No own this


---

3️⃣ Template Literals

let name = "John";
console.log(`Hello ${name}`);

✔️ Supports multiline strings
✔️ Variable interpolation


---

4️⃣ Default Parameters

function greet(name = "Guest") {
  return `Hello ${name}`;
}


---

5️⃣ Destructuring ⭐

Array

let [a, b] = [10, 20];

Object

let user = { name: "Alex", age: 25 };
let { name, age } = user;


---

6️⃣ Spread Operator (...)

let arr1 = [1,2];
let arr2 = [...arr1, 3, 4];

let obj1 = { a:1 };
let obj2 = { ...obj1, b:2 };


---

7️⃣ Rest Parameter (...)

function sum(...nums) {
  return nums.reduce((a,b) => a + b);
}


---

8️⃣ Enhanced Object Literals

let name = "Sam";
let user = {
  name,
  greet() {
    console.log("Hello");
  }
};


---

9️⃣ Classes ⭐

class Person {
  constructor(name) {
    this.name = name;
  }

  greet() {
    console.log(this.name);
  }
}


---

🔟 Inheritance

class Student extends Person {
  constructor(name, grade) {
    super(name);
    this.grade = grade;
  }
}


---

1️⃣1️⃣ Modules (Import / Export)

// export
export const add = (a,b) => a + b;

// import
import { add } from "./math.js";


---

1️⃣2️⃣ Promises ⭐⭐⭐

const promise = new Promise((resolve, reject) => {
  resolve("Success");
});

promise.then(res => console.log(res))
       .catch(err => console.log(err));


---

1️⃣3️⃣ Async / Await

async function fetchData() {
  let res = await fetch(url);
  let data = await res.json();
}

✔️ Cleaner async code
✔️ Built on Promises


---

1️⃣4️⃣ Map & Set

let map = new Map();
map.set("a", 1);

let set = new Set([1,2,2,3]);

✔️ Map → key-value
✔️ Set → unique values


---

1️⃣5️⃣ for...of Loop

for (let val of arr) {
  console.log(val);
}


---

1️⃣6️⃣ Symbol

let id = Symbol("id");

✔️ Unique identifiers


---

1️⃣7️⃣ Optional Chaining (?.)

user?.address?.city;

✔️ Prevents runtime errors


---

1️⃣8️⃣ Nullish Coalescing (??)

let name = userName ?? "Guest";

✔️ Checks only null or undefined


---

🎯 ES6 Interview One-Liners

ES6 → Modern JavaScript standard

Arrow Function → No own this

Destructuring → Extract values easily

Spread Operator → Copy & merge

Promise → Async handling



---

🚀 Want Next?

I can give you: ✔️ ES6 vs ES5 Comparison Table
✔️ ES6 Interview Questions (100+)
✔️ ES6 for React Cheat Sheet
✔️ ES6 Mini Coding Tasks

Just tell me 👍
