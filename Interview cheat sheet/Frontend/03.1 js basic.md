Here’s a clean, exam-ready + interview-friendly JavaScript Cheat Sheet 🔥
Perfect for freshers, full-stack learners, and quick revision.


---

⚡ JavaScript (JS) Cheat Sheet

1️⃣ JS Basics

// Single line comment
/* Multi-line comment */

console.log("Hello JS");

Variables

var x = 10;     // old (avoid)
let y = 20;     // block scoped
const z = 30;   // constant


---

2️⃣ Data Types

Number, String, Boolean
null, undefined
Object, Array

typeof 10        // "number"
typeof "Hi"      // "string"


---

3️⃣ Operators

+ - * / %        // Arithmetic
== === != !==    // Comparison
&& || !          // Logical

👉 === checks value + type


---

4️⃣ Conditions

if (age > 18) {
  console.log("Adult");
} 
else if (condition2) {
  // Code 
}
else {
  console.log("Minor");
}

Switch

switch(day) {
  case 1: break;
  default: break;
}


---

5️⃣ Loops

for (let i = 0; i < 5; i++) {}

while (condition) {    update;}

do {      update; } while(condition);
 

---

6️⃣ Functions

function add(a, b) {
  return a + b;
}

Arrow Function

const add = (a, b) => a + b;


---

7️⃣ Arrays

let arr = [1, 2, 3];

arr.push(4);     // add
arr.pop();       // remove
arr.length;

Array Methods ⭐

arr.map(x => x * 2);
arr.filter(x => x > 2);
arr.reduce((a,b) => a+b);


---

8️⃣ Objects

let user = {
  name: "John",
  age: 25
};

user.name;
user["age"];


---

9️⃣ DOM Manipulation ⭐

document.getElementById("id");
document.querySelector(".class");

element.innerText = "Text";
element.innerHTML = "<b>HTML</b>";


1. Selecting Elements : Use these methods to find elements within the Document Object Model (DOM). 

document.getElementById('id'): Selects a single element by its unique ID.
document.querySelector('.selector'): Selects the first element matching a CSS selector (class, ID, or tag).
document.querySelectorAll('.selector'): Selects all elements matching a CSS selector (returns a static NodeList).
document.getElementsByClassName('name'): Selects all elements with a specific class.
document.getElementsByTagName('tag'): Selects all elements with a specific tag name (e.g., div, p). 

2. Modifying Content & Attributes
Methods for changing what is inside an element or its metadata. 
.textContent: Gets or sets the plain text content; ignores HTML tags.
.innerHTML: Gets or sets HTML markup inside an element.
.getAttribute('attr'): Returns the value of a specified attribute.
.setAttribute('attr', 'value'): Sets or updates an attribute's value.
.removeAttribute('attr'): Removes an attribute from the element.
.value: Gets or sets the value of form inputs (e.g., text fields, checkboxes). 

Events

button.addEventListener("click", () => {
  alert("Clicked");
});


---

🔟 Strings

let str = "Hello";

str.length;
str.toUpperCase();
str.includes("He");
str.split("");


---

1️⃣1️⃣ Template Literals

let name = "Alex";
console.log(`Hello ${name}`);


---

1️⃣2️⃣ Spread & Rest

let a = [1,2];
let b = [...a,3];

function sum(...nums) {}


---

1️⃣3️⃣ Destructuring

let [a,b] = [10,20];

let {name, age} = user;


---

1️⃣4️⃣ JSON

JSON.stringify(obj);
JSON.parse(jsonData);


---

1️⃣5️⃣ Asynchronous JS ⭐⭐⭐

setTimeout

setTimeout(() => {
  console.log("Hi");
}, 1000);

Promise

new Promise((resolve, reject) => {});

Async / Await

async function fetchData() {
  let res = await fetch(url);
  let data = await res.json();
}


---

1️⃣6️⃣ Error Handling

try {
  riskyCode();
} catch (e) {
  console.log(e);
} finally {}


---

1️⃣7️⃣ Local Storage

localStorage.setItem("key","value");
localStorage.getItem("key");
localStorage.removeItem("key");


---

1️⃣8️⃣ ES6+ Features ⭐

✔️ Arrow functions
✔️ let / const
✔️ Destructuring
✔️ Spread operator
✔️ Modules


---

🎯 JS Interview One-Liners

JS → Dynamic programming language

Closure → Function with preserved scope

Hoisting → Variable/function moved to top

Callback → Function passed as argument

Promise → Handles async operations



---

🚀 Want More?

I can provide: ✔️ JavaScript Interview Questions (150+)
✔️ JS Mini Projects (Beginner → Advanced)
✔️ JS → React Mapping Cheat Sheet
✔️ DOM + Async Deep Notes

Just tell me 👍
